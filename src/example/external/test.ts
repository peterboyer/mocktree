export const describe = (name: string, fn: () => void) => {
	void name;
	void fn;
};

export const test = (name: string, fn: () => void) => {
	void name;
	void fn;
};

export const it = test;

export const render = (component: unknown) => {
	void component;
};

export const renderHook = (hook: unknown) => {
	void hook;
};
